import React from 'react';
import { Button } from '@material-ui/core';

const Home = () => {
    return (
        <div className="home-nav">
            HOME PAGE
        </div>
    );
};

export default Home;